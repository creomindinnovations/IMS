{
  "name": "ims-firebase-migrate",
  "private": true,
  "type": "module",
  "scripts": {
    "migrate": "node migrate.mjs",
    "migrate:firestore": "node migrate.mjs --firestore-only",
    "migrate:storage": "node migrate.mjs --storage-only",
    "migrate:auth": "node migrate.mjs --auth-only"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0"
  }
}
